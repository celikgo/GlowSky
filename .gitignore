# Python
__pycache__/
*.py[cod]
.venv/
.venv313/
*.egg-info/
.pytest_cache/

# Env & secrets — NEVER commit
.env
*.key

# Validation run artifacts. docs/VALIDATION.md IS committed (it is the published
# report and CI checks it against a fresh run); the raw results are not.
validation-results.json
validation-junit.xml

# Coverage artifacts (CI uploads coverage.xml as a run artifact instead)
.coverage
coverage.xml
htmlcov/

# Local data
*.db
*.sqlite3
glowsky.db

# IDE
.idea/
.vscode/

# Claude Code — personal/local settings
.claude/settings.local.json
