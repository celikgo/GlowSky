{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the Glowsky main window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
