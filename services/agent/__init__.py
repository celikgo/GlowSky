"""Agent orchestration: LLM plans, the typed tool registry computes.

The registry is the seam where Phase 3 custom/registered tools plug in, and the
only path through which the agent touches chemistry.
"""
